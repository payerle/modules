#!/bin/bash

do_cmd()
{	cmd="$@"
	echo $MPS1 $cmd
	$cmd
}

do_cmd module purge
do_cmd module load bar/avx2
do_cmd module list
do_cmd bar
do_cmd module switch bar bar/avx
do_cmd module list
do_cmd bar
do_cmd module switch bar bar/sse4.1
do_cmd module list
do_cmd bar
